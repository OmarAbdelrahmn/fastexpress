export const metadata = {
  title: 'Rider Salary Statements | Express Service',
  description: 'Upload rider salaries and create an individual salary statement PDF for every matched rider.',
};

export default function RiderSalaryLayout({ children }) {
  return children;
}

