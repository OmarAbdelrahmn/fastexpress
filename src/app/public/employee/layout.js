export const metadata = {
  title: 'Employee Portal | بوابة الموظف',
  description: 'Upload your employee documents | رفع وثائق الموظف',
};

export default function PublicEmployeeLayout({ children }) {
  return <>{children}</>;
}
